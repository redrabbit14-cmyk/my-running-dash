import streamlit as st
import pandas as pd

st.title("🏃‍♂️ 러닝 크루 대시보드 (테스트)")

# 데이터 예시
data = {
    "이름": ["용남", "주현", "유재", "재탁"],
    "주간거리": [45.2, 38.5, 20.0, 15.3]
}
st.table(pd.DataFrame(data))

st.success("기본 화면이 정상적으로 뜹니다! 이제 API 연동을 진행해볼까요?")
